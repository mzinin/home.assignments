SET (TARGET_NAME manual_test)

SET (SOURCES
    "main.cpp"
)

ADD_EXECUTABLE (${TARGET_NAME}
    ${SOURCES}
)

TARGET_LINK_LIBRARIES (${TARGET_NAME}
    orders
)

INSTALL (
    TARGETS ${TARGET_NAME}
    RUNTIME DESTINATION ${TEST_OUTPUT_DIR}
)
